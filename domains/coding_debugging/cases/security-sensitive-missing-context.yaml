case_id: coding_debugging.security_sensitive.missing_context.v1
description: Security-sensitive coding tasks require security context.
domain: coding_debugging
subtype: code_review
version: "2026.3"

inputs:
  task_kind: code_review
  stack: "TypeScript + Node.js"
  objective: "Review a security-sensitive authentication patch."
  review_artifact: "Patch touching auth middleware."
  security_sensitive: true

expected:
  domain: coding_debugging
  subtype: code_review
  validation:
    should_pass: false
    expected_errors:
      - "Missing required input(s): security_context"
