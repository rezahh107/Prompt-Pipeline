case_id: coding_debugging.code_review.missing_artifact.v1
description: Code review requires a review artifact.
domain: coding_debugging
subtype: code_review
version: "2026.3"

inputs:
  task_kind: code_review
  stack: "TypeScript + Node.js"
  objective: "Review a patch for correctness."

expected:
  domain: coding_debugging
  subtype: code_review
  validation:
    should_pass: false
    expected_errors:
      - "Missing required input(s): review_artifact"
