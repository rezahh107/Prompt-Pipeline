{% if has_source_photo %}
Stage 1 image model task:
Edit the uploaded source photo into a premium realistic {{ style_description | default('Persian academic award portrait') }}.

{% if (subject_count | default(0)) > 0 %}
Priority order:
1. Preserve all visible people and their identities.
2. Preserve faces and natural appearance.
{% if has_document %}3. Preserve the {{ document_type | default('document') }}.
4. Change only the presentation layer.
5. Reserve overlay-safe space if needed.
{% else %}3. Change only the presentation layer.
4. Reserve overlay-safe space if needed.
{% endif %}

Protected:
- Same people count and identities.
- Faces, age impression, skin tones, hairstyles, clothing, body proportions, posture, hands, and natural relative positions.
{% if has_document %}- The {{ document_type | default('document') }} in the subject's hand: prominent, natural, rectangular, as sharp as the source allows, and not redesigned or replaced.{% endif %}

Editable:
- Background.
- Lighting.
- Color grading.
- Framing, only if it keeps heads, shoulders, hands{% if has_document %}, and the {{ document_type | default('document') }}{% endif %} visible.
{% endif %}
{% else %}
Stage 1 image model task:
Create a professional {{ style_description | default('image') }}.
{% endif %}

New background: {{ background_description | default('softly blurred modern academic office') }}.
- {{ color_palette.primary | default('warm cream') }} walls
- subtle {{ color_palette.secondary | default('navy') }} accents
- restrained {{ color_palette.accent | default('gold') }} details
- calm formal educational mood

Composition: vertical {{ aspect_ratio | default('4:5') }}.
{% if needs_overlay %}
Leave the top {{ safe_area_percent | default(25) }}% completely clean and empty for later deterministic overlay.
Keep all faces{% if has_document %} and the {{ document_type | default('document') }}{% endif %} below that area.
{% endif %}

Style: photorealistic, natural skin tones, soft warm professional lighting, subtle depth of field, dignified, publication-ready.

{% if needs_overlay %}
Do not generate any text, logo, QR code, ranking, score, certificate text, or typographic element.
{% endif %}
