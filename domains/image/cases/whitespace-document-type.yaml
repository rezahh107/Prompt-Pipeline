case_id: image.whitespace_document_type.v1
description: Whitespace-only document_type should be treated as missing.
domain: image
subtype: identity_preserving_edit
version: "2026.3"

inputs:
  image_type: portrait
  subject_count: 1
  has_source_photo: true
  has_document: true
  document_type: "   "

expected:
  domain: image
  subtype: identity_preserving_edit
  validation:
    should_pass: false
    expected_errors:
      - "Missing required input(s): document_type"
