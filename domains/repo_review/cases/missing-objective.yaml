case_id: repo_review.missing_objective.v1
description: Objective is required for repository review prompts.
domain: repo_review
subtype: repository_audit
version: "2026.3"

inputs:
  review_subject: "repository target"
  review_scope: repository

expected:
  domain: repo_review
  subtype: repository_audit
  validation:
    should_pass: false
    expected_errors:
      - "Missing required input(s): objective"
