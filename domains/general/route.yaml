domain: general
version: "2026.3"

subtypes:
  - id: default
    description: General-purpose prompt generation.
    is_default: true
    templates:
      primary: general.j2

risk_overrides:
  - condition: "exact_factual_claims == true"
    risk: medium
