domain: prompt_audit
version: "2026.3"

subtypes:
  - id: default
    description: Audit an existing prompt.
    templates:
      primary: audit.j2

risk_overrides:
  - condition: "domain == 'prompt_audit'"
    risk: medium
    note: "Prompt audits can surface reliability, safety, and evidence risks."
